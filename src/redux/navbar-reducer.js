let initialState = {
    Friends: [
        { id: 1, name: 'Andrey' },
        { id: 2, name: 'Anna' },
        { id: 3, name: 'Viktor' },
    ]
};

const navbarReducer = (state = initialState, action) => {
    return state;
}


export default navbarReducer;